spring.application.name=luxus-suites-management

# Perfil por defecto para desarrollo local.
# En Railway se debe configurar SPRING_PROFILES_ACTIVE=prod.
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}